# Silver: `query_platform_sku_group_id`

Грейн группы: `query,platform,sku_group_id`; `date` исключена из имени группы.

- [`query_skg_daily_conversions_legacy`](query_skg_daily_conversions_legacy/v1/README.md) — дневные legacy-конверсии поиска.
