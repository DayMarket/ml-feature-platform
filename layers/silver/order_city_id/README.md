# Silver: `order_city_id`

Грейн группы: `order_city_id`; `date` исключена из имени группы.

- [`order_completion_city_features`](order_completion_city_features/v1/README.md) — доли выкупа и невыкупа заказов по городу доставки.
