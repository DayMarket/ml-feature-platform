# Trino-source layer pipeline (Airflow/Python + pyiceberg), not Spark.
table:
  key: order_completion_category_features
  catalog: iceberg
  schema: silver
  name: feature_platform_order_completion_category_features
  primary_key: date,category_level,category_id
  meta:
    team: team:search

source:
  engine: trino
  trino_conn_id: trino_search
  # Окно по generated_at внутри снапшота analyze_date.
  lookback_days: 120

runtime:
  image: ghcr.io/daymarket/airflow:3.1.8-python3.11-ml-2

dag:
  id: feature-platform.layers.silver.category_level_category_id.order_completion_category_features
  group_tag: order-completion-rates
  schedule: "0 3 * * *"
  start_date: "2026-04-01T00:00:00Z"
  team: search
  owner: team:search

alerts:
  team: search
  severity: P3
  oncall_webhook_conn_id: oncall_webhook_search

dq:
  trino_conn_id: trino_search
  partition_date_template: '{{ data_interval_start.in_timezone("UTC").strftime("%Y-%m-%d") }}'
  tests:
    # Раскатка DQ идёт на витрины, часть которых пуста или отстала на недели.
    # Объёмные тесты и freshness — в warn: состояние фиксируется в
    # feature_platform_dq_results, но дежурного не будит. Перевод в error —
    # по мере починки витрины, с порогом по её реальной истории (AGENTS.md).
    - name: freshness
      severity: warn
    - name: row_count_min
      severity: warn
    - name: row_count_growth
      severity: warn

feature_stats:
  trino_conn_id: trino_search
  # Дословно совпадает с dq.partition_date_template: обе таски обязаны
  # смотреть на одну партицию, иначе профиль посчитан не по тем данным.
  partition_date_template: '{{ data_interval_start.in_timezone("UTC").strftime("%Y-%m-%d") }}'
  exclude_columns: []
