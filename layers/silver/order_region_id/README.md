# Silver: `order_region_id`

Грейн группы: `order_region_id`; `date` исключена из имени группы.

- [`order_completion_region_features`](order_completion_region_features/v1/README.md) — доли выкупа и невыкупа заказов по региону доставки.
