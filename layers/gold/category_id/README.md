# Gold: `category_id`

Ключ группы: `category_id`; `date` остаётся в PK дневных снимков.

- [`buyout_online_category_features`](buyout_online_category_features/v1/README.md) — online-таблица категории для сервиса невыкупов: сглаженная выкупаемость категории и общая выкупаемость маркетплейса, подстановка для sku без заказов за 90 дней.
