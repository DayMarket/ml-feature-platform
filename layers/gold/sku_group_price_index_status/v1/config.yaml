resources:
  path: config/resources.yaml

table:
  key: sku_group_price_index_status
  catalog: iceberg
  schema: gold
  name: feature_platform_sku_group_price_index_status
  primary_key: date,sku_group_id
  meta:
    team: team:search
