# Gold: `key_type_key_id`

Грейн группы: `key_type, key_id` — длинный формат «уровень × ID»; `date` остаётся в PK дневных снимков.

- [`buyout_item_signal_features`](buyout_item_signal_features/v1/README.md) — товарный сигнал выкупаемости по уровням sku / product / category / shop / brand в окнах 30 и 90 дней.
